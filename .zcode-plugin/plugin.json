{
  "name": "zcode-computer-use",
  "version": "4.0.0",
  "description": "macOS GUI control for ZCode: per-window screenshots plus an indexed accessibility tree with diff markers, element-first actions with coordinate fallback, background observe, verified text selection, and 4-way scroll. Single static Go binary — MCP server over stdio plus a cuu CLI with one JSON-printing verb per tool.",
  "author": {
    "name": "Santiago Sainz"
  },
  "license": "MIT",
  "keywords": [
    "computer-use",
    "macos",
    "accessibility",
    "gui-automation",
    "screen"
  ],
  "skills": "skills",
  "mcpServers": {
    "zcode-computer-use": {
      "command": "${ZCODE_PLUGIN_ROOT}/cuu/bin/cuu",
      "args": [
        "serve"
      ],
      "cwd": "${ZCODE_PROJECT_DIR}",
      "env": {
        "ZCODE_CUA_DATA": "${ZCODE_PLUGIN_DATA}"
      }
    }
  }
}
